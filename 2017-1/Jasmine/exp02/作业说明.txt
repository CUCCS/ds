第一个 数制转换

随机生成一个小于1024的数，通过程序生成对应的八进制数

第二个 括号匹配

手动输入一串字符串，如“（{ }）”“（））[ ]”程序判断输入的括号是否能两两一组按顺序匹配，输出匹配成功“matched！”或不成功“not matched！”

第三个 行编辑程序

通过输入一段代码，#表示退格，@表示清空本行，/表示输入结束，程序输出实际有效代码。

input：
whli##ilr#e(s#*s)
 outcha@putchar(*s=#++);/

output:
while(*s)
putchar(*s++);