﻿为了节省空间和时间，所有的操作都是在一开始建立的链表上实现的，无需建立新的空间去储存分解开的两个链表，而是将原来的链表剪断再进行重新连接的过程。

先随机生成一个长度大于五小于十的链表，将它打印出来，表头储存的数据为链表长度；

在将表中奇数位的元素进行重新连接，创建一个新的表头head，然后将偶数位的元素依次连接在head之后；

通过GetLength函数返回两条新链表的长度，并储存在头结点的data成员中；

分别打印两条新链表的长度和成员元素；

程序结束。